# Force legacy peer deps to resolve dependency conflicts
legacy-peer-deps=true

# Disable package-lock generation to prefer yarn.lock
package-lock=false

# Use exact versions to avoid dependency resolution issues
save-exact=true